[build]
# Disable sccache (binary not found on this machine)
rustc-wrapper = ""

[env]
FFMPEG_DIR = "C:\\tools\\ffmpeg-8.1.2-full_build-shared"

# Auto-inject ffmpeg DLLs into PATH when running examples/tests
[target.x86_64-pc-windows-msvc]
runner = ".cargo/run_with_ffmpeg.cmd"
